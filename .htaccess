<IfModule mod_rewrite.c>
    RewriteEngine On
    # Si alguien entra a staging.tu-dominio.com/sistema, 
    # internamente el servidor va a staging.tu-dominio.com/sistema/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>